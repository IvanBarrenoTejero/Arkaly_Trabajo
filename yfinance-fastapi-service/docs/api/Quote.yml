info:
  name: Quote
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/v1/quote/MSFT
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
