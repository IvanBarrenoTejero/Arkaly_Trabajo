info:
  name: History
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/history/MSFT?period=7d
  params:
    - name: period
      value: 7d
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
